// One glyph for "there is no reading here", so a table row that mixes a
// percentage, a duration, a rate and a timestamp shows the same absence in
// every column. Halfwidth: the fullwidth dash forms belong to Chinese prose,
// not to a data cell that renders identically under both locales.
export const NO_READING = '-';
